# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリの現状

**スキャフォールド進行中。** モノレポの骨組み（チケット 002）まで完了し、`apps/`（web・api）と `packages/`（api-spec・api-client）の4パッケージをルートから `turbo` で流せる状態。

**アプリケーションのコードはまだ1行も無い。** 各パッケージの `build` はダミー（`dist/` にファイルを吐くだけ）で、turbo の依存順序とキャッシュを検証するための仮置き。実体は 003（Go）・004（TypeSpec）で差し替える。

進行中の作業は `docs/tickets/README.md` を見ること。当面のゴールはスキャフォールド（「本番URLの `/health` が 200 を返す」まで）。

### コマンド

| 目的 | コマンド |
| --- | --- |
| ツールチェーンを揃える（初回・clone 後） | `mise install`（未信頼と言われたら先に `mise trust`） |
| 依存のインストール | `pnpm install` |
| 全パッケージのビルド | `pnpm exec turbo run build` |
| 全パッケージのテスト | `pnpm exec turbo run test` |
| 特定パッケージだけ | `pnpm exec turbo run build --filter @repo/web` |
| キャッシュを無視して実行 | `pnpm exec turbo run build --force` |
| 脆弱性チェック | `pnpm audit --audit-level=high` |

Node と pnpm のバージョンは `mise.toml` が決める（[ADR 20260812-1324](./docs/adr/20260812-1324-version-pinning-and-audit.md)）。**`pnpm` を直接叩く前に `mise install` が済んでいること。**

Go のテスト実行、コード生成、単一テストの実行方法は 003 / 004 で確定したらここに追記する。

## 役割分担（最重要・例外なく守る）

**Claude Code はこのリポジトリのアプリケーションコードとテストコードを書かない。実装は開発者本人が行う。**
学習をプロダクト完成と並ぶ目的に据えているため（[ADR 20260812-0905](./docs/adr/20260812-0905-development-workflow.md)）。

| Claude がやること | Claude がやらないこと |
| --- | --- |
| チケットの作成（`docs/tickets/`）— 事前調査込み | Edit / Write で `docs/` の外を触る（コード・テスト・**設定ファイル**） |
| 詰まったときのヒント | 「先に進めておく」「ついでに直しておく」 |
| 本人が書いたコードのレビュー | 頼まれていないチケットへの着手 |
| ADR・ドキュメントの起草 | 設計判断を先回りして決める |
| 公式ドキュメントの確認、環境調査 | |

**Claude が Edit / Write してよいのは `docs/` 配下だけ。** `mise.toml` / `go.mod` / `package.json` / `turbo.json` のような**設定ファイルも本人が書く**。ADR で決めたことを反映するのが目的でも、Claude が直接書き換えない——**何を・どのファイルの・どこに書くかを提示して、本人に打たせる**。
（実例: ADR で決めた Go のバージョンを Claude が勝手に `mise.toml` に書き込み、「あなたは実装しないで、何をどうするべきかをステップバイステップで教えて」と指摘を受けた）

**例外は、本人が明示的に「これは書いて」と指示したときのみ。**
「実装して」「作って」と読める曖昧な依頼は、**実装ではなくチケット化として応じ、それでよいか確認する**。

### 支援のルール

1. **事前調査に載せるコードは骨組みまで** — 型・シグネチャ・構造体は提示してよい。**ロジックと設計判断は空欄にする**。完成コードを載せない
   （根拠: Go は本人が業務で使う言語であり学習対象ではない。学習対象は設計・インフラ・DB・認証・ログ設計の方）
2. **段階的にヒントを出すのは「設計判断」に対してだけ。** 方向性 → 該当箇所 → 擬似コード → 答え、と刻むのは「どの層に置くか」「何を公開するか」のような**本人が決めるべきこと**に限る。**標準ライブラリの使い方・言語の構文・API のシグネチャは、最初から具体的に示す**（根拠はルール1と同じ）。**「わからない」と2回言われたら刻みすぎ**——その時点で骨組みと1行ずつの説明まで出す
3. **テストコードは本人が書く** — Claude はチケットに「テスト観点のリスト」を書くに留める
4. **完了条件は検証可能な形で書く** — コマンドと期待結果。「〜できていること」で終わらせない
5. **寄り道をしない。** レビューでも解説でも、**いま前に進めるために必要なことだけ**を書く。「後で効いてくる論点」（Linter を入れたときの話、言語仕様の細則、いま書かなくてよいテスト観点）は**その場では出さない**。残す必要があるものはチケットや ADR の「未決」に落とす。**目的はアプリを完成させることであって、網羅ではない**
   （実例: `ping.go` のレビューで `w.Write` の戻り値・引数の未使用ルール・コメント規約・追加のテスト観点を一度に並べ、「いろいろやりすぎ。私はアプリを作りたい」と指摘を受けた）
6. **選択肢を出す前に、本人の構想を聞く。** 「どうしますか」より先に「考えている形はありますか」
   （実例: ディレクトリ構成の選択肢を2つ提示したが、本人はレイヤー構造を用意しており、どちらも当てはまらなかった）
7. **区切りごとに現在地を示す。** 「チケットのどのステップか / 何が終わったか / 何が残っているか」を定期的に出す。**本人に「今何をしているの？」と聞かせない**

### 解説のルール

チケットを消化することより、**本人がその仕組みを自分の言葉で説明できるようになること**が目的（[ADR 20260812-0905](./docs/adr/20260812-0905-development-workflow.md)）。以下はすべて、実際に本人から指摘を受けて追加したもの。

1. **初出の語を、説明せずに使わない。** 対象はファイル名（`.node-version`）、設定キー（`save-exact` / `devEngines`）、ツール名（Corepack / mise）、概念（PATH / bin / shim / ワークスペース / task）——**すべて**。専門用語を別の専門用語で説明しない
   - 説明の型: **何をするものか → なぜ要るのか → 似た仕組みとの違い → いつからある仕組みか**
   - 「〜という仕組みです」で止めず、**何を解決するために生まれたか**まで書く
   - 同じ構造の説明を2回目に使うときは、既に説明した仕組みとの対比で示す（例:「mise が `.node-version` を読むのと同じ関係」）
   - **コマンドの引数に出す文字列も対象**（例: `-o bootstrap` の `bootstrap`）。**後から「これ何？」と聞かれたら、それは説明不足のサイン**
2. **コマンドを出すときは、実行すると何が起きるかを添える。** とくに**副作用**（グローバル設定の書き換え、他プロジェクトへの影響、不可逆な操作）は実行前に明示する
3. **手順は「1コマンド = 1目的」まで刻む。** 判断を挟む箇所で区切り、完了報告を待ってから次へ進む。複数ステップをまとめて渡さない
4. **手を動かす前に、公式ツールで代替できないか確認する。** 使わない場合は理由を述べる（例: `create-turbo` はテンプレートの構成が ADR の決定と合わないため不採用）
5. **座学で終わらせない。** 理屈を説明したら、**手元で確かめる手順**を添える。とくに「間違えるとどうなるか」の対比実験は理解が定着する（例: `outputs` を空にして、キャッシュヒットしても成果物が復元されないことを目視する）
6. **レビューでも、理解の答え合わせでも、正しい箇所を明示する。** 誤りの指摘だけを返さない。本人が自分の言葉で整理してきたときは、**合っている箇所を先に挙げてから、ずれている箇所だけを直す**
7. **横に広げず、縦に掘る。** いま書いているコードの中心にある概念（`http.ServeMux` / `lambda.Start` / `bootstrap` など）は、**成り立ち・設計意図・落とし穴まで含めて厚く**書く。逆に、そこから派生する周辺の話題には触れない。**説明の総量を減らすのではなく、配分を変える**——薄い説明を横に並べるのが最も嫌われる
   （実例: `mux` を1〜2行で済ませたまま先に進み、「mux の説明が少なすぎる。もうちょっとちゃんと解説してほしい」と指摘を受けた。同じ回に「寄り道が多すぎる」とも言われている——**この2つは同時に成立する**）

### チケット運用

- 置き場は `docs/tickets/NNN-<内容>.md`、書式は `docs/tickets/TEMPLATE.md`、一覧は `docs/tickets/README.md`
- 1枚 **2〜3時間**（週5時間なので週1〜2枚）
- **チケットは着手する直前に作る。** まとめて先に作らない（先のチケットは前の決定に依存し、事前調査がすぐ陳腐化するため）
- チケットを追加・完了したら `docs/tickets/README.md` の一覧表も更新する

## プロダクトの判断基準

機能や仕様を提案・実装するときは、常にこの問いに答える。

> **「この機能は、パンとの新しい出会いを増やすか？」**

No なら作らない。迷ったら `docs/requirements.md` の「4. やらないこと」を先に読む（食べログのパン版にしない、長文レビュー中心にしない、飲食店全般に広げない、店舗検索を中心にしない）。

## アーキテクチャ（決定済み・ADR に根拠あり）

```text
Cloudflare（配信層）   Next.js フロント / R2（画像・地図タイル）/ CDN・WAF・レート制限
        ↕
AWS（計算層）          Go の API — Lambda（VPC外）+ Function URL（API Gateway を使わない）
        ↕
データ層（AWSの外）     PostgreSQL + PostGIS（v0.1: Neon シンガポール → v1.0前に東京へ移行）
```

この構成は**連鎖している**。上流を覆すと下流が全部変わる（`docs/adr/README.md` の「決定の連鎖」参照）。

```text
バックエンド言語 = Go
  ├─→ Cloudflare Workers が使えない → API は AWS Lambda
  │      └─→ VPC を避けるため DB は AWS の外（NAT Gateway 月$32 / RDS Proxy 月$21 の回避が理由）
  └─→ フロントと型が切れる → TypeSpec → OpenAPI → 両側コード生成
```

**「Cloudflare 完結（TypeScript + Workers + D1）」はコスト・レイテンシでは客観的に優れている**が、Go を優先して却下済み（`docs/adr/20260812-0805`）。この案を再提案するなら、Go 採用の ADR を覆す話であることを明示すること。

## 実装時に必ず守る制約

いずれも ADR で理由づけされた制約。破ると後から高くつく。

- **DB は移行可能性を保つ** — Neon 固有機能に依存しない / PostGIS は標準機能のみ / 接続情報は環境変数で抽象化 / ORM ではなく **sqlc で生 SQL**、方言に寄らない標準 SQL に保つ / 本番スキーマはマイグレーションファイルで管理
- **API 契約の単一情報源は TypeSpec** — 手で OpenAPI や生成コードを編集しない。生成物はコミットし、CI で再生成して差分が出たら失敗させる
- **画像は署名付き URL でブラウザから R2 へ直接アップロード** — API 経由で画像本体を通さない（AWS の egress とリクエストサイズ上限の回避）
- **EXIF の GPS 情報は必ず除去する**（プライバシー要件・必須）
- **ユーザーの現在地はサーバーに保存しない** — 検索条件として都度受け取るだけ。公開される位置情報は「店舗の位置」であって「投稿者の位置」ではない
- **地理空間検索はインデックスを使う** — 全件スキャンをしない（p95 500ms 目標）
- **0 件の画面を出さない** — 周辺検索が 0 件なら半径を段階的に拡大し、それでも 0 件なら未投稿のパン屋を「未開拓」として表示する
- **OSM 由来の店舗とユーザー追加の店舗は、データソースを区別して保持する**

## 開発の進め方

- 作業は**チケット単位**で進める（上記「役割分担」参照）
- **TDD**。テストを先に書く
- コードには `How`、テストコードには `What`、コミットログには `Why`、コードコメントには `Why not` を書く
- **意思決定をしたら `docs/adr/<yyyymmdd-hhmm>-<内容がわかる名前>.md` を作成する。** 書式とルールは `docs/adr/README.md`（却下した案とその理由を必ず残す。既存 ADR は書き換えず、新しい ADR を作って古い方から参照する）
  - **例外: まだコミットしていない ADR は書き直してよい。** 履歴に残っていない以上、失われる経緯がないため。コミット済みのものは必ず新しい ADR を作る
  - **実装中に決定の前提が崩れたら、その場で ADR を直す。** 実装を優先して記録を後回しにしない（例: turbo が `devEngines.packageManager` を要求したため、「`packageManager` を書かない」という決定をその場で修正した）
- ADR を追加したら `docs/adr/README.md` の一覧と `docs/tech-decision-guide.md`「5. 決定済みの事項」も更新する
- ドキュメントは日本語

### 技術選定の判断軸

選定を提案するときは、`docs/tech-decision-guide.md` の 6 軸で評価する。衝突したら **要件適合 → 可逆性 → 開発時間 → 運用コスト → 学習の一般性 → 既知からの距離** の順。

**開発に割ける時間は週 5 時間、開発者は 1 名。** 初期構築や学習に時間を食う選択肢は、それ自体が不採用理由になる。過剰な設計を持ち込まない。

**公式ツールが生成した既定値を、手で書き換える提案をしない。** 一般論としての整理が正しくても、**この構成で実際に何が良くなるかを示せないなら、既定値のままにする**（[ADR 20260812-1305](./docs/adr/20260812-1305-go-in-turborepo-workspace.md) の「公式パターンを正とし、独自の工夫を持ち込まない」の適用範囲）。
（実例: `go mod init` が書いた `go 1.26.6` を「最低要求バージョンなのだから `1.26.0` にすべき」と提案したが、開発者1名・CI も同じ `mise.toml` を読む本プロジェクトでは利益がゼロで、指摘を受けて撤回した — [ADR 20260815-1420](./docs/adr/20260815-1420-go-version-and-module-path.md)）

## リリース段階とスコープ

| | 位置づけ | 入るもの |
| --- | --- | --- |
| **v0.1** | 非公開・自分専用。シードデータ作成ツール | 認証、写真投稿、店舗紐付け・**新規店舗追加**、パン種類、自分の投稿一覧、OSM 取り込みバッチ |
| **v1.0** | 一般公開 | タイムライン（距離＋鮮度）、周辺表示、地図（未投稿店舗も表示）、保存、プロフィール、通報・削除 |
| v1.1〜 | 対象外 | AI 自動タグ付け、コメント／フォロー、ゲーミフィケーション、店舗連携 |

機能を追加する前に、それが今の段階のスコープかを `docs/requirements.md`「6. 機能要件」で確認すること。

## 主要な未決事項

勝手に決めず、決めるときは ADR を書く。

- 認証方式と実装手段（v0.1 の投稿機能の前）
- Go の DB アクセス／マイグレーション管理（DB 設計時）
- PostGIS のどの機能を使うか（DB 設計時）
- 画像リサイズをクライアント側で行うか Lambda 側か（投稿機能の実装時）
- 地図タイルの配信元 — OSM 公式タイルサーバーは本番利用不可（v1.0 の地図機能の前）
- DB の最終的な移行先（PlanetScale の PostGIS 復旧状況を確認してから）
- 商標・ドメインの実査 — **v1.0 公開の準備を始める前まで繰り延べ済み**（[ADR 20260814-1826](./docs/adr/20260814-1826-defer-trademark-research.md)）。v0.1 の間は名称を「**仮の COPAN**」として扱う。**ドメイン・SNS アカウントは取得しない**（不可逆なため）
