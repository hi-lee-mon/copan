# 002: Turborepo のモノレポ骨組みを作る

- ステータス: 未着手
- 見積: 3h / 実績: -h
- 依存: 001（名称確定。リポジトリ名・パッケージ名に影響する）
- 関連: [ADR 20260811-2015 Turborepo](../../adr/20260811-2015-monorepo-turborepo.md)、[技術選定ガイド G-1 / G-2](../tech-decision-guide.md)

## 1. 目的

`apps/` と `packages/` を持つワークスペースを作り、**リポジトリのルートから `turbo` でタスクを流せる状態**にする。

## 2. 背景

[ADR 20260811-2015](../../adr/20260811-2015-monorepo-turborepo.md) で Turborepo を採用済み。同ADRの「結果として必要になる決定」に残っている **G-2（パッケージマネージャ）とディレクトリ構成** を、このチケットで確定させる。

以降のチケット（TypeSpec の生成、Go のビルド、CI）はすべてこの上に載るため、ここで構成を決めておかないと後続が置き場所に困る。

## 3. 事前調査

### ワークスペースと Turborepo の役割分担

混同しやすいので先に分けておく。

| | 担当 |
| --- | --- |
| **パッケージマネージャのワークスペース**（pnpm / npm） | 依存の解決とインストール。パッケージ間の参照 |
| **Turborepo** | **タスクの依存グラフとキャッシュ。** 依存解決はしない |

Turborepo はパッケージマネージャの代替ではなく、その上に乗る**タスクランナー**。

### G-2 の選択肢

現在の環境には pnpm 10.13.1 と npm（Node 24.4.0 同梱）の両方がある。

| | pnpm | npm workspaces |
| --- | --- | --- |
| インストール方式 | コンテンツアドレス可能なストア＋シンボリックリンク | フラットな巻き上げ |
| 未宣言の依存 | **参照できない**（厳密） | 巻き上げにより参照できてしまう |
| ディスク・速度 | 有利 | 不利 |
| 追加インストール | 必要 | 不要 |

**「未宣言の依存を参照できてしまう」問題**が、モノレポで効いてくる論点。判断は自分で。

### turbo.json の要点

タスクと、その依存関係を宣言する。理解しておくべき記法はこれ。

```jsonc
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],   // ← この "^" が何を指すかを調べる
      "outputs": []              // ← キャッシュ対象の成果物を書く
    },
    "test": {},
    "lint": {}
  }
}
```

`outputs` の指定を誤ると**キャッシュが効かない、あるいは壊れた成果物を復元する**。ここは調べる価値がある。

### ディレクトリ構成の骨組み（中身は自分で決める）

```text
copan/
├── apps/
│   ├── web/          # Next.js
│   └── api/          # Go ← 論点あり（下記）
├── packages/
│   ├── ???/          # TypeSpec の定義
│   └── ???/          # 生成された TS の型・APIクライアント
├── package.json
├── turbo.json
└── <ワークスペース定義ファイル>
```

**Go をどう置くかが最大の論点。** Go は Go modules で依存を管理するため、npm/pnpm のワークスペースには含まれない。一方で Turborepo からはタスク（`go build` / `go test`）として叩きたい。この2つをどう両立させるかを調べる。

> 生成物の置き場所（`packages/` の2つ）は 004 で TypeSpec に触れてから決めた方が精度が上がる。**このチケットでは空のディレクトリと名前だけ決めればよい。**

## 4. 学習TODO

- [ ] `dependsOn: ["^build"]` の `^` が何を指すか説明できる
- [ ] Turborepo のキャッシュが何をキーに算出されるか説明できる
- [ ] `outputs` を指定し忘れると何が起きるか説明できる
- [ ] Go のディレクトリを npm ワークスペースに含められない理由を説明できる
- [ ] pnpm の「厳密な依存解決」が何を防ぐのか説明できる

## 5. 不足情報TODO

- [ ] **G-2 パッケージマネージャを決める → ADR を作成する**
- [ ] apps/ と packages/ の切り方を決める → ADR 20260811-2015 を書き換えず、新ADRか本チケットの記録として残す
- [ ] Node.js のバージョン固定方法（`engines` / `packageManager` / `.node-version` のどれを使うか）
- [ ] Turborepo からGoのタスクを叩く方法（`apps/api/package.json` を置くのか、別の手段か）

## 6. 実装ステップ

このチケットは設定ファイル中心でテストを書く対象が薄い。**「壊れていないこと」を確認する手段を先に決める**のが実質的な赤→緑にあたる。

1. **確認手段を決める**: ルートで `turbo run build` と `turbo run test` が成功することを、このチケットの検証コマンドとする
2. ワークスペースを初期化し、`apps/` `packages/` を作る
3. `turbo.json` にタスクを定義する
4. 各ワークスペースに最小のダミータスクを置き、ルートから流れることを確認する
5. 2回目の実行でキャッシュがヒットすることを確認する（`FULL TURBO` 表示）
6. `.gitignore` が新しい構成と整合しているか確認する

### テスト観点

- ルートからタスクが全ワークスペースに流れるか
- 2回目の実行でキャッシュがヒットするか
- 依存関係のあるタスクが正しい順序で走るか
- 1つのワークスペースを変更したとき、無関係なワークスペースがキャッシュでスキップされるか

## 7. 完了条件

- [ ] ルートで `turbo run build` が成功する（中身が空でもよい）
- [ ] 同じコマンドを2回目に実行するとキャッシュがヒットする
- [ ] G-2（パッケージマネージャ）の ADR が存在する
- [ ] `.gitignore` が新しいディレクトリ構成と整合している
- [ ] 学習TODOがすべて埋まっている

## 8. 振り返り（完了時に本人が記入）

- 詰まった点:
- 分かったこと:
- 見積とのズレと、その原因:
